//3.

/**
 * arrow function
 *
 *
 */
// 1 Template string.
const getfifty = () => 55;
const addSicty = (num) => num + 65;
const isEven = (x) => x % 2 == 0;
const add = (a, b, c) => x + y + z;
const doMathe = (num1, num2) => {
  // retrn for this code ;
};
// 2. arrow functon
// sparad oprator
const numbers = [10, 20, 30, 40];
const newNumbers = [...numbers, 7];

console.log(newNumbers);
numbers.push(99);
console.log(newNumbers);
//3rd --
const Student = {
  name: "sujon",
  age: 34,
  movies: ["king kahan", "Dhaker maskan"],
};
const about = `my Name is ${Student.name}  my movi is king kahan ${Student.movies}`;
console.log(about);
